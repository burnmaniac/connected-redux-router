export const selectRouterState = state => state.router;

export const selectRouterLocation = state => state.router.location;

export const selectRouterAction = state => state.router.action;

export const selectRouterSearch = state => state.router.search;

export const selectRouterHash = state => state.router.hash;
